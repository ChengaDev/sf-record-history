import { React } from "react";

const Grid = (rows) => {
    return (
        <div id="grid">
            <div>
                Grid Header
            </div>
        </div>
    );
};

export default Grid;